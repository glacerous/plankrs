{
    "framework": "nextjs",
    "installCommand": "corepack enable && corepack prepare pnpm@latest --activate && pnpm -w install --frozen-lockfile",
    "buildCommand": "cd apps/web && pnpm build",
    "outputDirectory": "apps/web/.next"
}