{
    "name": "krsplan",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npx pnpm --filter krsplan-engine build --watch & npx pnpm --filter web dev",
        "build:packages": "npx pnpm --filter krsplan-engine build && npx pnpm --filter @krs/engine build",
        "build:web": "npx pnpm --filter web build",
        "build": "npx pnpm run build:packages && npx pnpm run build:web",
        "install:all": "npx pnpm install"
    },
    "devDependencies": {
        "typescript": "^5.7.2"
    }
}